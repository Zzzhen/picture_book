<view class="segment">
  <block wx:for="{{options}}" wx:key="value">
    <button class="segment__item {{value === item.value ? 'segment__item--active' : ''}}" data-value="{{item.value}}" bindtap="onSelect">{{item.label}}</button>
  </block>
</view>
