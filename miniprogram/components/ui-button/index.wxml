<button
  class="button button--{{type}} button--{{size}} {{block ? 'button--block' : ''}} {{disabled || loading ? 'button--disabled' : ''}}"
  disabled="{{disabled || loading}}"
  loading="{{loading}}"
  bindtap="onTap"
>
  {{loading ? '处理中' : text}}
</button>
