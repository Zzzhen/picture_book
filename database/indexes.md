# 数据库索引

机器可读清单见 `database/indexes.json`。部署时必须先在 development 创建并验证，再同步 production。

重点索引覆盖：用户状态与创建时间、孩子 owner、公共版本搜索 token/前缀、手工审核状态与冲突、ISBN 缓存过期、个人藏书 owner/排序/偏好、书架顺序、书架关系、事件、反馈、审计、额度、幂等与注销任务。

索引名称和字段顺序属于 schema 版本的一部分；控制台手工变更后必须同步回 JSON 并新增迁移记录。
