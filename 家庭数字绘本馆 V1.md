# 家庭数字绘本馆 V1.0 核心建馆版开发规格

> 文档状态：已完成产品与技术重构，待产品方最终复核  
> 当前编码版本：`V1-Core`（核心建馆版）  
> 下一阶段版本：`V1-Share`（分享运营版，仅保留规划，当前禁止编码）  
> 产品形态：微信小程序  
> 前端技术：原生微信小程序 JavaScript、WXML、WXSS  
> 后端技术：微信云开发的云函数、云数据库、云存储  
> 外部服务：阿里云云市场「聚美智数 ISBN 书号查询」  
> UI/Figma 规格：`家庭数字绘本馆 UI与Figma设计规范.md`  
> 最后更新：2026-07-28

---

## 1. 文档用途

本文档是家庭数字绘本馆微信小程序的产品、技术与验收主规格。GPT 或其他编码助手应联合读取本文档和 `家庭数字绘本馆 UI与Figma设计规范.md`，完成 `V1-Core` 的项目初始化、页面开发、云函数开发、数据库配置、测试和交付。

本文档中的“必须”“禁止”“默认”均为强制要求。凡标记为 `V1-Share`、`下一阶段` 或 `ROADMAP_ONLY` 的内容仅用于保证架构可演进，当前阶段不得生成对应页面、云函数、集合、入口或模拟数据。

### 1.1 GPT 编码规则

编码助手必须遵守以下顺序：

1. 阅读完整文档后再生成代码。
2. 只实现第 3.1 节 `V1-Core` 范围，并按第 20 节的阶段顺序实施，一次只完成一个阶段。
3. 每个阶段先列出将创建或修改的文件，再开始编码。
4. 每个阶段结束后运行对应测试并汇报结果。
5. 不得把阿里云 AppCode、OpenID 或其他敏感信息写入前端、代码仓库或日志。
6. 不得引入独立服务器、独立数据库、传统后端框架或需要长期运维的服务。
7. 不得使用 React、Vue、Taro、uni-app 或 Web UI 组件库。
8. UI 必须按独立设计文档逐页实现，页面层级、组件、变量、状态、文案和原型行为不得自行改写。
9. 开始 UI 编码前，可以使用 Frontend Design/UI Skill 做视觉自检，但 Skill 不得覆盖设计文档、业务规则、隐私规则和微信小程序技术约束。
10. 每个云函数 action 必须按第 15 节契约实现，不得自行增加或重命名字段。
11. 遇到文档冲突时，优先级为：当前版本边界 > 安全与隐私 > action 契约 > 数据模型 > UI/Figma 设计文档 > 描述性文字。

---

## 2. 产品目标

### 2.1 一句话定位

帮助家长逐步录入、整理、欣赏和分享家庭已有绘本的数字绘本馆。

### 2.2 当前版本要验证的核心闭环

```text
强制完成首次建馆
→ 扫码、连续扫码或手工录入已有绘本
→ 形成统一美观的数字书架
→ 创建自定义书架并做轻量偏好标记
→ 继续补充和整理绘本
```

`V1-Share` 将在此闭环后增加“创建分享 → 获得浏览和点赞反馈 → 访客转化建馆”，但不属于本阶段验收。

### 2.3 目标用户

- 核心用户：拥有 0—6 岁儿童、家中有几十至数百本绘本的家长。
- 典型需求：盘点藏书、快速找书、自定义分类、展示家庭阅读积累。
- 次级用户：绘本博主、绘本 KOC 和愿意分享家庭书架的家长。

### 2.4 产品原则

- 不要求用户首次录完全部绘本。
- 不做阅读次数、阅读时长或每日打卡。
- 用户拍摄或上传的图片是输入素材，不直接作为未经处理的公共展示素材。
- 前台操作保持简单，复杂数据处理放在云函数中。
- 下一阶段仅支持主动分享，不建设公共社区或推荐信息流。
- 私人备注、孩子精确出生年月和负面偏好默认永不公开。

---

## 3. 版本范围

### 3.1 `V1-Core` 当前必须实现

1. 微信身份初始化、用户资料和账号状态。
2. 首次建馆；孩子昵称、出生年月和性别均为必填。
3. 一个主要孩子档案；当前不提供多孩子管理。
4. ISBN 单次扫码、手工输入和连续扫码模式。
5. 云数据库缓存优先、阿里云 ISBN 接口回源和标准化结果落库。
6. 阿里云临时封面转存至微信云存储。
7. 缓存书库搜索；只搜索曾经查询并已审核入库的书，搜不到时显示空结果页面。
8. 手动录入兜底、1—3 天审核提示、驳回原因和重新编辑。
9. 封面网格与管理列表两种绘本馆视图。
10. 绘本详情、拥有数量、私人备注和偏好标记。
11. 自定义书架的创建、编辑、排序和批量加书。
12. 搜索、筛选、排序和批量管理个人藏书。
13. 小程序内隐藏的基础管理员模块。
14. 手工图书审核、ISBN 冲突提示和管理员版本保留决策。
15. 核心行为埋点和基础指标展示，不设置成功阈值或自动结论。
16. 账号注销和个人数据清理任务。

### 3.2 `V1-Share` 下一阶段规划（`ROADMAP_ONLY`）

下一阶段完整范围如下，当前编码助手只允许读取其边界，不得实现：

1. 分享整个绘本馆或指定书架。
2. 分享前隐私选择、公开内容预览和快照发布。
3. 公共分享页、失效页、浏览量、独立访客数。
4. 绘本馆级点赞与取消点赞。
5. 分享访客进入小程序并创建自己的绘本馆。
6. 管理员关闭违规分享。
7. 分享指标卡和转化指标。

下一阶段开始前必须单独确认页面级 Figma 设计、接口契约、数据集合和验收清单。当前版本只允许在命名和数据模型说明中预留扩展点，不创建 `shareService`、分享页面或分享集合。

### 3.3 当前及下一阶段均明确不开发

- 公共社区、发现页或推荐信息流。
- 关注、评论、私信或用户搜索。
- 阅读打卡、阅读次数、阅读时长。
- AI 阅读人格分析或复杂推荐算法。
- 二手交易、支付、物流、会员体系。
- 单本绘本点赞；下一阶段也只允许绘本馆级点赞。
- 拍整面书架后自动识别全部绘本。
- 独立 Web 管理后台。
- 数据导出。
- 分享长图海报。
- 多个孩子的前台管理界面。

### 3.4 `V1-Core` 管理能力边界

管理员功能放在同一小程序的隐藏路由中，仅 `users.role === "admin"` 的用户可以访问。管理员可以：

- 查看用户基本状态和藏书数量。
- 禁用或恢复用户。
- 审核、编辑手动录入的绘本。
- 修正 ISBN、书籍信息和封面。
- 重试封面转存。
- 查看基础业务指标。
- 查看 ISBN 冲突，选择保留已有版本、用新版本覆盖标准字段，或拒绝新版本；任何决策都必须记录审计日志。

管理员账号由产品负责人在 `production` 环境首次发布前，通过云开发控制台把指定用户的 `role` 手工改为 `admin`。前端和仓库中不得存在硬编码管理员 OpenID。

手工录入审核目标为提交后 1—3 个自然日。产品不承诺精确完成时间，但用户端必须始终显示该预期。驳回必须填写原因，用户可以修改后重新提交。

不建设单独网站，不购买服务器或数据库。

---

## 4. 技术架构

### 4.1 架构总览

```text
微信小程序
  ├─ 页面与组件
  ├─ 本地状态和缓存
  └─ services/api.js
          │ wx.cloud.callFunction
          ▼
微信云函数
  ├─ userService
  ├─ bookService
  ├─ libraryService
  ├─ bookshelfService
  ├─ eventService
  ├─ adminService
  └─ maintenanceService
          │
          ├─ 微信云数据库
          ├─ 微信云存储
          └─ 阿里云 ISBN API
```

### 4.2 强制技术选择

- 小程序：微信开发者工具原生项目。
- 语言：JavaScript。
- 云函数：使用微信云开发当前支持的 Node.js LTS 运行时；`package.json#engines` 和锁文件固定实际版本，升级需先在 `development` 验证。
- 云函数 SDK：`wx-server-sdk`。
- 数据库：微信云开发文档型数据库。
- 文件：微信云存储。
- 身份：云函数运行上下文中的 `OPENID`。
- 测试：纯函数和云函数核心模块使用 Node.js 内置 `node:test`。
- 网络请求：仅云函数可以请求阿里云 ISBN API。

### 4.3 安全边界

- 前端不直接写任何数据库集合。
- 私人数据和公共分享数据均通过云函数读取。
- 客户端传入的 `user_id`、`role`、`owner_id` 一律不可信。
- 云函数必须从运行上下文获取 OpenID，再查询用户和权限。
- 数据库安全规则默认禁止小程序端直接读写，云函数使用服务端权限访问。
- 管理接口必须同时验证 OpenID、用户状态和 `role === "admin"`。
- 云函数调用安全规则默认要求已登录用户；`adminService` 还必须在函数内部验证管理员角色。
- `maintenanceService` 禁止小程序客户端直接调用，只允许定时触发器或云端调用。
- 所有 action 都在函数内部执行 action 白名单、身份、账号状态、资源所有权和字段白名单校验。

`database/function-security-rules.md` 必须记录并部署等价规则：

```json
{
  "*": {
    "invoke": "auth.loginType != 'ANONYMOUS' && auth != null"
  },
  "maintenanceService": {
    "invoke": false
  }
}
```

所有当前集合的数据库客户端规则均为 `read: false`、`write: false`。如果未来开放任何客户端直读，必须单独评审并更新查询条件、索引和规则测试。

### 4.4 用户标识与密钥

- 不直接保存 OpenID，也不使用裸 `SHA-256(OpenID)`。
- `user_id = "u_v1_" + HMAC-SHA256(USER_ID_SECRET, OPENID)`。
- `USER_ID_SECRET` 按环境独立配置在云函数环境变量中，不进入前端、仓库或日志。
- 所有确定性关系 ID 使用 UTF-8 字符串 `v1|关系类型|字段1|字段2` 计算 SHA-256，禁止使用无分隔符字符串拼接。
- 更换 `USER_ID_SECRET` 必须通过迁移脚本进行，禁止直接替换导致现有账号失联。

### 4.5 环境划分

至少建立两个微信云开发环境：

- `development`：本地开发和体验版。
- `production`：正式版。

小程序使用 `config/env.js` 保存非敏感的环境 ID 映射。阿里云 AppCode 只保存在对应云函数的环境变量中。

云函数环境变量清单：

| 名称 | 作用 |
| --- | --- |
| `ALIYUN_ISBN_APPCODE` | ISBN 供应商鉴权 |
| `ALIYUN_ISBN_ENDPOINT` | ISBN 接口地址 |
| `USER_ID_SECRET` | 生成不可逆用户 ID |
| `ISBN_PROVIDER_TIMEOUT_MS` | 供应商超时，默认 5000 |
| `COVER_SOURCE_HOST_ALLOWLIST` | 允许下载封面的逗号分隔域名 |
| `COVER_MAX_BYTES` | 封面最大字节数，默认 5242880 |
| `ISBN_USER_DAILY_LIMIT` | 用户日调用上限，默认 100 |
| `ISBN_GLOBAL_DAILY_LIMIT` | 系统日调用上限，默认 3000 |
| `ISBN_GLOBAL_MONTHLY_LIMIT` | 系统月调用上限，默认 50000 |

生产发布脚本只校验变量是否存在，不输出变量值。

### 4.6 云函数共享代码打包

`cloudfunctions/_shared` 是源代码目录，不是可直接部署的独立云函数。仓库必须提供 `scripts/build-cloudfunctions.js`：

1. 清理并重新生成 `dist/cloudfunctions`。
2. 把每个业务云函数及其 `package.json` 复制到独立部署目录。
3. 把 `_shared` 复制到每个业务云函数的 `_shared` 子目录。
4. 安装或锁定生产依赖。
5. 对生成目录运行 Node 语法检查和契约测试。

微信开发者工具只部署 `dist/cloudfunctions/{functionName}`。禁止让业务函数以 `../_shared` 依赖部署目录外文件。

### 4.7 云函数资源与触发器

| 云函数 | 超时 | 内存 | 客户端可调用 |
| --- | --- | --- | --- |
| `userService` | 10 秒 | 256 MB | 是 |
| `bookService` | 20 秒 | 512 MB | 是 |
| `libraryService` | 10 秒 | 256 MB | 是 |
| `bookshelfService` | 10 秒 | 256 MB | 是 |
| `eventService` | 10 秒 | 256 MB | 是 |
| `adminService` | 20 秒 | 512 MB | 是，函数内限管理员 |
| `maintenanceService` | 60 秒 | 512 MB | 否 |

定时任务使用 `maintenanceService`：

- `processDeletionJobs`：每 10 分钟。
- `cleanupLookupCache`：每 15 分钟。
- `checkProviderQuota`：每小时。
- `cleanupEphemeralData`：每天 03:15。
- `reconcileCounts`：每天 02:30。

具体 cron 表达式记录在 README 和云函数配置文件中，时区固定为 `Asia/Shanghai`。

---

## 5. 项目目录

```text
/
├─ miniprogram/
│  ├─ app.js
│  ├─ app.json
│  ├─ app.wxss
│  ├─ sitemap.json
│  ├─ assets/
│  │  ├─ icons/
│  │  ├─ images/
│  │  └─ placeholders/
│  ├─ components/
│  │  ├─ app-header/
│  │  ├─ book-cover/
│  │  ├─ book-grid/
│  │  ├─ book-list-item/
│  │  ├─ bookshelf-card/
│  │  ├─ empty-state/
│  │  ├─ filter-sheet/
│  │  ├─ preference-picker/
│  │  ├─ skeleton/
│  │  └─ toast/
│  ├─ config/
│  │  ├─ env.js
│  │  └─ constants.js
│  ├─ pages/
│  │  ├─ bootstrap/
│  │  ├─ onboarding/
│  │  ├─ library/
│  │  ├─ add-book/
│  │  ├─ book-confirm/
│  │  ├─ book-detail/
│  │  ├─ bookshelves/
│  │  ├─ bookshelf-detail/
│  │  ├─ bookshelf-edit/
│  │  ├─ manual-book-edit/
│  │  ├─ profile/
│  │  ├─ profile-edit/
│  │  ├─ feedback/
│  │  └─ admin/
│  ├─ services/
│  │  ├─ api.js
│  │  ├─ auth.js
│  │  ├─ cache.js
│  │  └─ upload.js
│  ├─ styles/
│  │  ├─ tokens.wxss
│  │  ├─ utilities.wxss
│  │  └─ animations.wxss
│  └─ utils/
│     ├─ isbn.js
│     ├─ date.js
│     ├─ debounce.js
│     ├─ validators.js
│     └─ error-message.js
├─ cloudfunctions/
│  ├─ _shared/
│  │  ├─ auth.js
│  │  ├─ response.js
│  │  ├─ errors.js
│  │  ├─ validators.js
│  │  ├─ pagination.js
│  │  └─ audit.js
│  ├─ userService/
│  ├─ bookService/
│  ├─ libraryService/
│  ├─ bookshelfService/
│  ├─ eventService/
│  ├─ adminService/
│  └─ maintenanceService/
├─ tests/
│  ├─ unit/
│  ├─ contract/
│  └─ fixtures/
├─ database/
│  ├─ collections.md
│  ├─ indexes.md
│  ├─ database-security-rules.md
│  ├─ function-security-rules.md
│  └─ migrations/
├─ scripts/
│  ├─ build-cloudfunctions.js
│  ├─ create-indexes.js
│  └─ seed-development.js
├─ dist/
│  └─ cloudfunctions/            # 构建产物，不手工编辑
├─ project.config.json
├─ 家庭数字绘本馆 UI与Figma设计规范.md
└─ README.md
```

禁止把单个页面、组件或云函数写成超大文件。页面逻辑超过约 300 行时，应提取服务、校验器或组件。

---

## 6. 信息架构与页面

### 6.1 底部导航

底部导航固定为三个入口：

1. 绘本馆。
2. 书架。
3. 我的。

“添加绘本”使用绘本馆右下角的主要悬浮按钮，同时在空状态中提供主按钮。`V1-Core` 不制作复杂的自定义中间凸起 TabBar。

### 6.2 页面清单

| 页面 | 路由 | 主要职责 |
| --- | --- | --- |
| 启动页 | `pages/bootstrap/index` | 初始化云环境、识别用户、决定跳转 |
| 初次建馆 | `pages/onboarding/index` | 设置绘本馆名称和主要孩子资料 |
| 我的绘本馆 | `pages/library/index` | 网格/列表、搜索、筛选、排序和添加 |
| 添加绘本 | `pages/add-book/index` | 单次扫码、连续扫码、输入 ISBN、搜索缓存书库、手动添加 |
| 确认绘本 | `pages/book-confirm/index` | 展示匹配结果、确认加入以及连续扫码的“继续扫描” |
| 绘本详情 | `pages/book-detail/index` | 标准信息、数量、偏好、书架、备注和删除 |
| 书架列表 | `pages/bookshelves/index` | 展示、新建和排序自定义书架 |
| 书架详情 | `pages/bookshelf-detail/index` | 展示书架内绘本、排序和批量管理 |
| 编辑书架 | `pages/bookshelf-edit/index` | 名称、简介和选书 |
| 编辑手工图书 | `pages/manual-book-edit/index` | 查看审核状态、驳回原因、修改并重新提交 |
| 我的 | `pages/profile/index` | 用户、孩子、统计、隐私、反馈和注销 |
| 编辑资料 | `pages/profile-edit/index` | 修改昵称、头像、孩子资料和城市 |
| 意见反馈 | `pages/feedback/index` | 提交反馈 |
| 管理入口 | `pages/admin/index` | 管理员功能容器 |

### 6.3 下一阶段页面清单（`ROADMAP_ONLY`）

以下页面只用于下一阶段排期，本阶段不创建目录：

| 页面 | 预留路由 | 下一阶段职责 |
| --- | --- | --- |
| 分享设置 | `pages/share-edit/index` | 分享范围、标题、简介、隐私开关和选书 |
| 分享预览 | `pages/share-preview/index` | 发布前预览公开快照 |
| 公共分享页 | `pages/share-view/index` | 展示公开快照、PV、UV、点赞和建馆入口 |
| 分享失效页 | `pages/share-closed/index` | 分享关闭或删除后的明确状态 |
| 我的分享 | `pages/my-shares/index` | 管理已发布分享和查看指标 |

### 6.4 关键页面状态

每个列表型页面必须实现以下状态：

- 首次加载骨架屏。
- 正常内容。
- 空状态。
- 无搜索结果。
- 网络或云函数失败。
- 下拉刷新。
- 分页加载。
- 已加载全部。
- 无权限或账号已禁用。

禁止只使用全屏“加载中”文字。请求超过 300 毫秒才显示加载反馈，避免闪烁。

---

## 7. UI 与交互设计系统

全部页面级设计、变量、组件、状态、文案、图层命名和 Figma 原型要求，统一定义在：

> `家庭数字绘本馆 UI与Figma设计规范.md`

强制规则：

1. 当前视觉方向为“A · 家藏书票”：暖纸底色、森林绿、陶土色，以及克制使用的家庭藏书票印章。
2. Figma MCP 必须先创建 Foundations 和 Components，再创建页面；禁止先画页面后补组件。
3. 编码助手不得自行增加近似颜色、圆角、阴影、字号或图标风格。
4. 微信小程序实现以设计文档中的页面 ID、组件 ID、状态矩阵和原型连线为验收依据。
5. `V1-Share` 页面在设计文档中仅保留清单和流程边界，本阶段不得生成完整高保真页面。

---

## 8. 身份、用户和孩子资料

### 8.1 身份初始化

小程序启动后调用 `userService.bootstrap`：

1. 云函数从运行上下文获取 OpenID。
2. 按第 4.4 节计算 `user_id = "u_v1_" + HMAC-SHA256(USER_ID_SECRET, OPENID)`，以该值作为 `users._id` 查找用户。
3. 不存在则创建最小用户记录。
4. 返回用户状态、角色和是否完成建馆。
5. 前端根据结果进入引导页、绘本馆或禁用提示页。

不要求用户先授权昵称和头像才能浏览自己的空绘本馆。昵称和头像由用户主动填写或选择。

### 8.2 首次建馆字段

必填：

- 绘本馆名称，默认建议为“我的绘本馆”。
- 孩子昵称。
- 孩子出生年月。
- 孩子性别，可选值为 `female`、`male`、`unspecified`。

选填：

- 用户昵称。
- 用户头像。
- 妈妈年龄段。
- 所在城市。

前台只动态计算孩子年龄段，不保存持续变旧的整数年龄。

### 8.3 隐私默认值及下一阶段公开预留

- 用户昵称：不在分享页显示，除非用户开启。
- 孩子昵称：不显示，除非用户开启。
- 孩子年龄：只允许显示年龄段，不显示出生年月。
- 城市：不显示，除非用户开启。
- 妈妈年龄段：永不在分享页显示。
- 私人备注：永不在分享页显示。

---

## 9. ISBN 查询与图书入库

### 9.1 服务信息

服务商页面：

<https://market.aliyun.com/detail/cmapi00053669>

接口：

```text
POST https://jmisbn.market.alicloudapi.com/isbn/query
Content-Type: application/x-www-form-urlencoded; charset=UTF-8
Authorization: APPCODE ${ALIYUN_ISBN_APPCODE}

isbn=9787555357902
```

环境变量：

| 名称 | 必填 | 说明 |
| --- | --- | --- |
| `ALIYUN_ISBN_APPCODE` | 是 | 阿里云云市场 AppCode，只配置在 `bookService` |
| `ALIYUN_ISBN_ENDPOINT` | 是 | 固定为接口地址 |
| `ISBN_PROVIDER_TIMEOUT_MS` | 是 | 默认 `5000` |
| `COVER_MAX_BYTES` | 是 | 默认 `5242880`，即 5 MB |

### 9.2 ISBN 标准化

前端和云函数都要校验，云函数校验为最终结果：

1. 去掉空格和连字符。
2. 将全角字符转为半角。
3. 校验 ISBN-10 或 ISBN-13 校验位。
4. ISBN-10 末位为 `X` 时先转换成 ISBN-13，再调用只接受纯数字的供应商接口。
5. 数据库主键统一使用 ISBN-13。
6. 非法 ISBN 返回 `INVALID_ISBN`，不调用付费接口。

### 9.3 缓存优先流程

```text
用户扫码或输入 ISBN
→ 规范化为 ISBN-13
→ 查询 book_editions/isbn_{isbn13}
→ 命中有效记录：直接返回，providerCalled=false
→ 未命中：检查 isbn_lookup_cache
→ 负缓存未过期：返回未找到，不调用供应商
→ 无缓存：获取该 ISBN 查询锁
→ 调用阿里云 ISBN API
→ 标准化数据
→ 先写入 book_editions 和 found 缓存
→ 释放查询锁
→ 尝试转存封面到微信云存储
→ 更新 cover_status 并返回
```

### 9.4 防重复付费调用

`isbn_lookup_cache` 使用 `_id = "isbn_" + isbn13`。

状态：

- `querying`：某个请求正在回源。
- `found`：已找到，对应 `edition_id`。
- `not_found`：未找到，24 小时后过期。
- `provider_error`：供应商错误，5 分钟后允许重试。

第一个请求只在短事务中把状态写为 `querying`，生成随机 `lock_token` 并设置 60 秒锁。外部 HTTP 请求和封面下载禁止放进数据库事务。并发请求发现有效锁时返回 `BOOK_LOOKUP_IN_PROGRESS`，前端使用 800、1600、2400 毫秒退避查询 `getLookupStatus`。只有持有相同 `lock_token` 的请求可以提交结果或释放锁。

供应商标准数据写入并把缓存改为 `found` 后，即视为付费查询已完成；封面转存失败不得把缓存恢复为 `querying`，从而避免重复付费。

### 9.5 调用限制

- 缓存命中不计入外部调用限制。
- 每个用户每天最多触发 100 次外部 ISBN 查询。
- 同一用户两次外部查询间隔至少 800 毫秒。
- 系统级默认每天最多 3000 次、每月最多 50000 次外部查询，实际值由环境变量配置。
- 达到系统额度 80% 时写告警日志；达到 100% 时打开熔断，只允许缓存命中和手工录入，由管理员手工解除。
- 单次连续扫码会话最多扫描 100 次，仍受用户级和系统级额度限制。
- HTTP 429、HTTP 5xx、超时或供应商业务码 `500` 最多自动重试一次。
- 供应商业务码 `400` 不重试。
- 供应商业务码 `999` 不自动重试，提示用户稍后再试或手动录入。

### 9.6 供应商返回结构

成功以响应 JSON 中的 `code === 200` 为准，不能只看 HTTP 状态码。

```json
{
  "code": 200,
  "msg": "成功",
  "taskNo": "供应商业务号",
  "data": {
    "details": [
      {
        "series": "",
        "title": "书名",
        "author": "作者、编者或译者",
        "publisher": "出版社",
        "pubDate": "出版日期",
        "pubPlace": "出版地",
        "isbn": "13位ISBN",
        "isbn10": "10位ISBN",
        "price": "28.80",
        "genus": "中图分类号",
        "levelNum": "读者评分",
        "heatNum": "热度",
        "format": "开本",
        "binding": "装帧",
        "page": "页数",
        "wordNum": "字数",
        "edition": "版次",
        "yinci": "印次",
        "paper": "纸张类型",
        "language": "语言",
        "keyword": "|关键词1|关键词2",
        "img": "24小时有效的封面链接",
        "bookCatalog": "目录",
        "gist": "内容简介",
        "cipTxt": "CIP信息",
        "annotation": "附注",
        "subject": "主题",
        "batch": "丛编信息"
      }
    ]
  }
}
```

失败示例：

```json
{
  "msg": "参数[isbn]不能为空",
  "code": 400
}
```

已知供应商业务码：

| 业务码 | 含义 | 本系统处理 |
| --- | --- | --- |
| `200` | 成功 | 校验 `details[0]` 后落库 |
| `400` | 参数错误 | 返回 `ISBN_PROVIDER_BAD_REQUEST` |
| `500` | 服务商维护 | 重试一次，仍失败则返回可恢复错误 |
| `999` | 其他错误 | 不重试，允许手动录入 |

### 9.7 字段映射

| 供应商字段 | `book_editions` 字段 |
| --- | --- |
| `title` | `title` |
| `author` | `contributors_text` |
| `publisher` | `publisher` |
| `pubDate` | `publish_date_text` |
| `pubPlace` | `publish_place` |
| `isbn` | `isbn13` |
| `isbn10` | `isbn10` |
| `price` | `price_text` |
| `genus` | `classification_code` |
| `format` | `format` |
| `binding` | `binding_type` |
| `page` | `page_count_text` |
| `edition` | `edition_text` |
| `yinci` | `printing_text` |
| `paper` | `paper_type` |
| `language` | `language` |
| `keyword` | `keywords`，去空值后保存为数组 |
| `img` | 仅作为转存输入，不作为永久前端地址 |
| `bookCatalog` | `catalog` |
| `gist` | `description` |
| `cipTxt` | `cip_text` |
| `annotation` | `annotation` |
| `subject` | `subject` |
| `series`、`batch` | `series_text`、`batch_text` |

不保存完整原始响应。仅保存 `provider_task_no`、`provider_code`、`provider_message`、响应字段白名单的 SHA-256 摘要和 `provider_queried_at`。需要排障时使用脱敏日志，日志保留期不超过 14 天。

### 9.8 封面转存

供应商说明 `img` 链接约 24 小时后失效，因此成功返回后必须在同一次云函数流程中：

1. 校验 URL 协议为 HTTPS，域名必须在 `COVER_SOURCE_HOST_ALLOWLIST` 中。
2. DNS 解析结果不得是回环、链路本地、内网或保留地址。
3. 最多跟随 2 次重定向，每次重定向后重新执行协议、域名和 IP 校验。
4. 下载超时 5 秒，并在响应头或流读取阶段拒绝超过 5 MB 的文件。
5. 同时校验响应 MIME、文件魔数和图片实际解码结果；只允许 JPEG、PNG、WebP。
6. 限制最长边不超过 6000 像素，总像素不超过 2400 万，拒绝图片解压炸弹。
7. 根据实际 MIME 类型确定扩展名。
8. 上传到 `book-covers/{isbn13}/{contentHash}.{ext}`。
9. 保存微信云存储 `fileID` 到 `cover_file_id`。
10. `cover_status` 写为 `ready`。

转存失败不能阻止图书信息落库。此时：

- `cover_status = "failed"`。
- 前端使用统一占位封面。
- 管理员可以执行 `retryCoverTransfer`。

### 9.9 搜索和手动录入

缓存书库搜索只搜索已经落入 `book_editions` 的数据，支持：

- ISBN 精确匹配。
- 书名前缀或包含匹配。
- 作者/贡献者匹配。
- 出版社匹配。

搜索不到时不调用阿里云接口，也不自动跳转手工录入。页面显示专用空结果状态：

> 已收录的绘本里还没有找到这本书  
> 你可以扫描 ISBN，或者手动录入。

空结果提供“扫描 ISBN”和“手动录入”两个操作。搜索实现使用标准化前缀字段和分词数组，不使用无法命中索引的任意数据库正则包含查询；详细字段见第 13 节。

没有结果时允许手动录入：

- 书名，必填。
- 作者，选填。
- 出版社，选填。
- ISBN，选填；填写后必须校验。
- 装帧，选填。
- 封面，选填。

手动记录：

- `source = "manual"`。
- `audit_status = "pending"`。
- 审核前仅创建者本人可在个人绘本馆中看到。
- 审核通过后才进入其他用户可搜索的缓存书库。
- 提交成功页和图书详情显示“预计 1—3 个自然日完成审核”。
- `pending` 和 `rejected` 记录禁止进入任何公共数据或下一阶段分享快照。
- 驳回时管理员必须填写 1—200 字原因；用户在详情页查看原因、修改并重新提交。
- 用户上传封面先通过微信内容安全图片检测，再进入管理员审核；检测不通过直接拒绝并显示可理解原因。
- 手工记录填写 ISBN 后，管理员审核页必须查询相同 `isbn13` 并展示冲突对比。
- 管理员可选择：保留现有标准版本并把用户藏书改关联到现有版本；采用新提交内容覆盖允许修改的标准字段；驳回新提交。
- 冲突处理必须在事务中完成关联迁移和审核状态更新，并写入 `audit_logs`；不得出现同一 ISBN 的两个已审核标准版本。
- 带 ISBN 且无冲突的提交审核通过时，事务创建 `isbn_{isbn13}` 标准版本、迁移引用该草稿的 `user_books`、更新书架关系中的确定性 ID，并把草稿标为已归并。
- 不带 ISBN 的提交审核通过后保留 `manual_` 版本 ID 并改为 `approved`。
- 迁移时如果同一用户已经拥有目标标准版本：数量相加但最多 99；保留目标记录的非 `unmarked` 偏好，否则采用来源偏好；两边备注都存在时以换行和“来自手工录入”分隔后合并；书架关系去重；`created_at` 取较早时间；来源藏书软删除。

### 9.10 连续扫码模式

用户在添加绘本页主动进入连续扫码模式：

1. 调用 `wx.scanCode` 获取 ISBN。
2. 查询成功后进入简化确认页，主操作为“加入并继续扫”，次操作为“加入并结束”。
3. 点击“加入并继续扫”后完成幂等入库，显示不超过 500 毫秒的成功反馈，再自动调起下一次扫码。
4. 扫到已拥有版本时显示当前数量，用户选择“数量加一并继续”或“跳过并继续”。
5. 查询失败、无结果或额度受限时停留在结果页，允许重试、手工录入、跳过继续或结束。
6. 用户取消系统扫码界面时返回连续扫码汇总页，不再次自动调起。
7. 汇总页显示本次新增、数量增加、跳过和失败数量，并可查看失败项。
8. 每次会话使用 `scan_session_id`；每个扫描结果生成独立 `requestId`，禁止用一次批量请求包住全部 ISBN。
9. 会话明细保存在当前页面状态和以 `scan_session_id` 为键的本地缓存，不创建云数据库集合；正常结束后清理，异常中断的缓存 24 小时后清理。

---

## 10. 个人绘本馆规则

### 10.1 藏书记录

同一用户和同一版本只保留一条 `user_books` 记录，通过 `quantity` 表示拥有数量。

重复录入时提示：

> 这本书已经在你的绘本馆中。

用户可以：

- 查看已有记录。
- 确认数量加一。
- 取消。

### 10.2 网格模式

- 默认模式。
- 小屏默认两列，标准和大屏三列，具体断点与封面适配见独立设计文档。
- 显示封面、书名和可选偏好标签。
- 点击进入详情。
- 长按打开快捷操作面板。

### 10.3 列表模式

- 显示小封面、书名、作者/出版社、偏好和所属书架数量。
- 支持多选。
- 适用于批量加书架和批量删除。

用户最后选择的视图保存在本地缓存和用户偏好字段中。

### 10.4 搜索、筛选和排序

搜索字段：

- 书名。
- 作者/贡献者。
- 出版社。
- ISBN。

筛选：

- 推荐、一般、不推荐、未标记。
- 自定义书架。
- 有封面/无封面。

排序：

- 最近录入。
- 最早录入。
- 书名。
- 用户自定义。

搜索输入使用 300 毫秒防抖。服务端对标准化 `search_prefixes` 和 `search_tokens` 查询；如果为了精确匹配使用正则，必须转义输入并仅允许锚定前缀。

### 10.5 偏好

枚举：

- `unmarked`：未标记。
- `recommended`：推荐。
- `neutral`：一般。
- `not_recommended`：不推荐。

单本只能有一个状态，可随时修改或清除。默认私人。

### 10.6 删除

删除个人藏书使用软删除：

- `deleted_at` 写入服务端时间。
- 同时从所有书架关系中移除。
- 下一阶段实现分享功能时，删除流程必须额外创建分享快照清理任务；当前阶段不创建该任务。

---

## 11. 自定义书架

### 11.1 规则

- 用户可以创建、重命名、编辑简介、删除和排序书架。
- 一本绘本可以属于多个书架。
- 删除书架不删除个人藏书。
- 书架名称长度为 1—20 个字符。
- 简介长度不超过 100 个字符。
- 每个用户最多创建 50 个书架。
- 每个书架最多加入 500 条不同藏书记录。
- 书架内支持拖动排序或上移/下移排序。

### 11.2 书架封面

`V1-Core` 不要求用户单独上传书架封面。书架卡片默认使用前四本有效绘本封面拼成组合封面；不足四本时使用品牌占位底图。

---

## 12. `V1-Share` 分享运营版边界（`ROADMAP_ONLY`）

本节只说明下一阶段产品闭环，不构成当前编码任务。

### 12.1 页面流程

```text
绘本馆或书架点击分享
→ 分享设置：选择公开绘本和允许公开的资料
→ 分享预览：只展示即将公开的快照
→ 发布：生成版本化公共快照
→ 好友打开公共分享页
→ 记录 PV、UV，允许绘本馆级点赞
→ 访客点击“创建我的绘本馆”
→ 进入当前版本的首次建馆流程
```

### 12.2 页面清单

- 分享设置。
- 分享预览。
- 公共分享页。
- 分享失效页。
- 我的分享与指标。
- 管理员违规分享处置。

### 12.3 后续设计与研发边界

- 只允许主动分享，不建设公共社区、发现页、关注、评论或私信。
- 发布数据使用版本化快照，公共接口不实时读取私人藏书。
- 私人备注、孩子出生年月、妈妈年龄段、联系方式、未选择绘本和偏好原始状态永不公开。
- 分享关闭或账号删除后，公共接口立即拒绝访问；快照清理由异步任务完成。
- 大型分享必须分批生成快照，不允许在一个长事务中处理全部绘本。
- PV、UV 和点赞先写事件，聚合计数可异步校正，避免热点文档竞争。
- 访客标识使用独立的 `VISITOR_HASH_SECRET` 做 HMAC，不与 `user_id` 使用同一密钥或同一输出。
- 下一阶段必须重新完成需求确认、Figma 页面设计、数据集合、接口 schema、安全规则、性能预算和验收测试后才能编码。

---

## 13. 云数据库设计

### 13.1 通用字段规则

- 时间统一使用数据库服务端时间。
- 字段使用 `snake_case`。
- 枚举保存英文小写值。
- 所有用户输入在云函数中执行去首尾空格、长度校验和类型校验。
- 私人集合必须包含 `owner_id`。
- 软删除字段统一为 `deleted_at`，未删除时为 `null`。
- 返回前端的数据必须使用白名单序列化，禁止直接返回整份数据库文档。

### 13.2 `users`

```json
{
  "_id": "u_v1_{HMAC-SHA256(USER_ID_SECRET, OPENID)}",
  "role": "user",
  "status": "active",
  "nickname": "",
  "avatar_file_id": "",
  "library_name": "我的绘本馆",
  "mother_age_range": "",
  "city": "",
  "onboarding_completed": false,
  "preferred_library_view": "grid",
  "created_at": "serverDate",
  "updated_at": "serverDate",
  "last_login_at": "serverDate",
  "deleted_at": null
}
```

`role`：`user`、`admin`。  
`status`：`active`、`disabled`、`deleting`、`deleted`。

原始 OpenID 不落库也不返回前端。所有业务云函数都从当前运行上下文重新计算 `user_id`，不得接受客户端提交的用户身份。

### 13.3 `children`

```json
{
  "_id": "自动生成",
  "owner_id": "users._id",
  "nickname": "一宝",
  "birth_year_month": "2023-06",
  "gender": "unspecified",
  "is_primary": true,
  "created_at": "serverDate",
  "updated_at": "serverDate",
  "deleted_at": null
}
```

### 13.4 `book_works`（下一阶段预留）

当前版本不创建此集合、不生成 `work_id`，也不做版本归并。未来确需把多个 ISBN 版本关联为同一作品时，先新增迁移和管理员交互规格。

### 13.5 `book_editions`

ISBN 图书使用确定性 ID：`isbn_{isbn13}`。

```json
{
  "_id": "isbn_9787555357902",
  "isbn13": "9787555357902",
  "isbn10": "7555357909",
  "title": "",
  "title_normalized": "",
  "contributors_text": "",
  "contributors_normalized": "",
  "publisher": "",
  "publisher_normalized": "",
  "search_prefixes": ["猜", "猜猜", "猜猜我"],
  "search_tokens": ["猜猜我有多爱你", "作者名", "出版社名", "9787555357902"],
  "publish_date_text": "",
  "publish_place": "",
  "language": "",
  "binding_type": "",
  "format": "",
  "edition_text": "",
  "printing_text": "",
  "page_count_text": "",
  "paper_type": "",
  "price_text": "",
  "classification_code": "",
  "series_text": "",
  "batch_text": "",
  "keywords": [],
  "subject": "",
  "description": "",
  "catalog": "",
  "cip_text": "",
  "annotation": "",
  "cover_file_id": "",
  "cover_status": "ready",
  "source": "aliyun_isbn",
  "audit_status": "approved",
  "created_by": "",
  "merged_into_edition_id": "",
  "provider_task_no": "",
  "provider_code": 200,
  "provider_message": "",
  "provider_response_hash": "",
  "provider_queried_at": "serverDate",
  "created_at": "serverDate",
  "updated_at": "serverDate",
  "deleted_at": null
}
```

无 ISBN 的手工版本使用 `_id = "manual_" + UUID`。带 ISBN 的手工提交在审核通过前也使用 `manual_` ID，审核时由管理员冲突决策决定是否迁移到 `isbn_{isbn13}`。`book_editions` 只保存当前可供个人藏书关联的规范化版本；审核过程和历史保存在 `manual_book_submissions`。

`audit_status`：`pending`、`approved`、`rejected`、`merged`。`merged` 必须填写 `merged_into_edition_id` 并软删除，所有业务读取重定向到规范版本。

搜索字段生成规则：

1. 对书名、贡献者和出版社执行 Unicode NFKC、英文小写、全角转半角、连续空白合并和首尾标点清理。
2. `search_prefixes` 保存各字段 1—20 个字符的前缀，去重后最多 60 项。
3. `search_tokens` 保存完整标准化值、ISBN，以及中文连续 2 字 token/英文按词 token，去重后最多 100 项。
4. 查询先用 ISBN、前缀或单个 token 命中索引，服务端最多读取 100 条候选，再对标准化全文做最终包含判断和排序。
5. 不允许无索引全表扫描；候选达到 100 条时返回排序后的前 50 条并提示用户输入更多文字。

### 13.6 `manual_book_submissions`

```json
{
  "_id": "submission_{UUID}",
  "owner_id": "users._id",
  "draft_edition_id": "manual_{UUID}",
  "isbn13": "",
  "submitted_fields": {
    "title": "",
    "contributors_text": "",
    "publisher": "",
    "binding_type": "",
    "cover_file_id": ""
  },
  "content_security_status": "passed",
  "status": "pending",
  "rejection_reason": "",
  "conflict_edition_id": "",
  "canonical_edition_id": "",
  "resolution": "",
  "reviewed_by": "",
  "submitted_at": "serverDate",
  "reviewed_at": null,
  "updated_at": "serverDate"
}
```

`status`：`pending`、`approved`、`rejected`、`merged`。  
`resolution`：空值、`keep_existing`、`replace_allowed_fields`、`reject_new`。

### 13.7 `isbn_lookup_cache`

```json
{
  "_id": "isbn_9787555357902",
  "isbn13": "9787555357902",
  "status": "found",
  "edition_id": "isbn_9787555357902",
  "lock_token": "",
  "lock_expires_at": null,
  "provider_code": 200,
  "expire_at": null,
  "created_at": "serverDate",
  "updated_at": "serverDate"
}
```

### 13.8 `user_books`

同一用户和版本使用确定性关系 ID。

```json
{
  "_id": "sha256('v1|user_book|' + owner_id + '|' + edition_id)",
  "owner_id": "users._id",
  "edition_id": "book_editions._id",
  "quantity": 1,
  "preference": "unmarked",
  "private_note": "",
  "custom_sort": 0,
  "search_prefixes": [],
  "search_tokens": [],
  "created_at": "serverDate",
  "updated_at": "serverDate",
  "deleted_at": null
}
```

### 13.9 `bookshelves`

```json
{
  "_id": "自动生成",
  "owner_id": "users._id",
  "name": "",
  "description": "",
  "sort_order": 0,
  "book_count": 0,
  "created_at": "serverDate",
  "updated_at": "serverDate",
  "deleted_at": null
}
```

### 13.10 `bookshelf_books`

```json
{
  "_id": "sha256('v1|bookshelf_book|' + bookshelf_id + '|' + user_book_id)",
  "owner_id": "users._id",
  "bookshelf_id": "bookshelves._id",
  "user_book_id": "user_books._id",
  "sort_order": 0,
  "created_at": "serverDate"
}
```

### 13.11 下一阶段集合预留（`ROADMAP_ONLY`）

当前版本禁止创建 `share_pages`、`share_books`、`share_likes`、`share_visits` 或分享聚合事件集合。下一阶段必须根据第 12 节重新确认最终 schema 和索引，不能把旧草案直接用于生产。

### 13.12 其他集合

#### `events`

保存第 18 节定义的埋点事件。只保存必要属性，不保存私人备注或完整 OpenID。

#### `feedback`

保存 `owner_id`、反馈类型、内容、联系方式选填项、处理状态和服务端时间。

#### `audit_logs`

保存 `admin_id`、操作对象、操作类型、变更摘要和服务端时间，不保存 AppCode。

#### `system_config`

保存可调整但非敏感的服务限制，例如每日 ISBN 外部调用上限、分页大小和维护开关。敏感密钥仍使用云函数环境变量。

#### `schema_migrations`

保存 `version`、迁移名称、checksum、执行环境、开始/完成时间和结果。每次 schema 或索引变更必须提供向前迁移脚本、回滚说明和 `development` 验证记录。

#### `rate_limits`

使用确定性 ID 保存用户级或系统级 ISBN 外部调用次数和最近调用时间。ID 统一通过 `sha256("v1|rate_limit|" + scope_id + "|" + limit_type + "|" + period_key)` 生成。

```json
{
  "_id": "确定性哈希",
  "scope_id": "users._id 或 global",
  "limit_type": "isbn_external",
  "period_key": "2026-07-27",
  "count": 1,
  "last_called_at": "serverDate",
  "expire_at": "次日之后的清理时间"
}
```

#### `idempotency_keys`

使用 `_id = sha256("v1|idempotency|" + owner_id + "|" + service + "|" + action + "|" + requestId)` 原子认领写请求，24 小时后由维护任务清理。

```json
{
  "_id": "确定性哈希",
  "owner_id": "users._id",
  "service": "libraryService",
  "action": "addBook",
  "request_id": "客户端UUID",
  "status": "completed",
  "response_code": "OK",
  "response_data": {
    "resource_id": "",
    "resource_version": 1
  },
  "created_at": "serverDate",
  "expire_at": "24小时后的时间"
}
```

`status`：`processing`、`completed`、`failed_retryable`。`response_data` 只允许保存恢复响应所需的资源 ID、版本和结果码，序列化后最大 4 KB，禁止保存个人资料、备注、供应商响应或完整业务文档。

#### `deletion_jobs`

保存账号注销后的数据清理任务和执行状态：

```json
{
  "_id": "自动生成",
  "owner_id": "users._id",
  "status": "pending",
  "cursor": {},
  "created_at": "serverDate",
  "updated_at": "serverDate",
  "completed_at": null
}
```

清理任务必须删除该用户的孩子、藏书、书架、书架关系、反馈、幂等记录和可识别事件；手工提交中的个人字段匿名化，但保留已被管理员采纳的公共标准书目及审计记录。不可识别的汇总指标可以保留。

注销提交后立即把 `users.status` 设为 `deleting` 并拒绝业务操作。24 小时内完成清理后保留最小账号墓碑：`_id`、`status = "deleted"`、`deleted_at`，其余字段清空。相同微信账号再次进入时必须显示“重新创建绘本馆”确认页；确认后复用该 `_id` 创建全新资料，不恢复已删除数据。

---

## 14. 数据库索引

必须创建以下组合索引：

| 集合 | 索引字段 |
| --- | --- |
| `children` | `owner_id + deleted_at` |
| `book_editions` | `isbn13` |
| `book_editions` | `title_normalized + audit_status` |
| `book_editions` | `contributors_normalized + audit_status` |
| `book_editions` | `search_prefixes + audit_status` |
| `book_editions` | `search_tokens + audit_status` |
| `manual_book_submissions` | `owner_id + status + submitted_at` |
| `manual_book_submissions` | `status + submitted_at` |
| `manual_book_submissions` | `isbn13 + status` |
| `user_books` | `owner_id + deleted_at + created_at` |
| `user_books` | `owner_id + preference + deleted_at` |
| `user_books` | `owner_id + edition_id + deleted_at` |
| `bookshelves` | `owner_id + deleted_at + sort_order` |
| `bookshelf_books` | `bookshelf_id + sort_order` |
| `bookshelf_books` | `owner_id + user_book_id` |
| `events` | `owner_id + event_name + created_at` |
| `feedback` | `status + created_at` |
| `feedback` | `owner_id + created_at` |
| `audit_logs` | `admin_id + created_at` |
| `isbn_lookup_cache` | `expire_at` |
| `rate_limits` | `expire_at` |
| `rate_limits` | `scope_id + limit_type + period_key` |
| `idempotency_keys` | `expire_at` |
| `deletion_jobs` | `status + created_at` |
| `schema_migrations` | `version` |

确定性 `_id` 用于实现唯一关系，不能只依赖“先查询再新增”。

---

## 15. 云函数接口契约

### 15.1 通用协议

```json
{
  "action": "lookupByIsbn",
  "payload": {},
  "requestId": "客户端生成的UUID"
}
```

```json
{
  "ok": true,
  "code": "OK",
  "message": "",
  "data": {},
  "requestId": "原样返回"
}
```

```json
{
  "ok": false,
  "code": "INVALID_ISBN",
  "message": "请输入正确的 ISBN",
  "data": null,
  "requestId": "原样返回"
}
```

规则：

- `action` 必须命中函数内白名单；未知 action 返回 `ACTION_NOT_FOUND`。
- 所有请求必须携带 UUID v4 `requestId`；写请求用其实现幂等。
- payload 未声明字段一律返回 `INVALID_ARGUMENT`，禁止静默接收。
- 时间返回 ISO 8601 UTC 字符串，数据库内部仍使用服务端时间。
- ID、角色和 owner 信息永远从云端身份与数据库获取，不出现在可写 payload 中。
- 云函数不得返回堆栈、数据库错误、供应商原始响应、密钥或内部锁 token。

### 15.2 公共数据类型

```text
PageInput = { cursor?: string, limit?: integer(1..50) }
PageResult<T> = { items: T[], next_cursor: string|null, has_more: boolean }

EditionSummary = {
  edition_id, isbn13, title, contributors_text, publisher,
  cover_file_id, cover_status, source, audit_status,
  price_text, page_count_text
}

UserBookSummary = {
  user_book_id, edition: EditionSummary, quantity, preference,
  bookshelf_count, created_at, updated_at
}

ShelfSummary = {
  bookshelf_id, name, description, sort_order, book_count, updated_at
}

ManualSubmissionSummary = {
  submission_id, draft_edition_id, status, rejection_reason,
  conflict_edition_id, submitted_at, reviewed_at
}
```

`cursor` 是服务端生成的 base64url 字符串，内部版本为 `v1` 并包含排序值与 `_id`。客户端只能原样回传，不得解析或拼接。

### 15.3 `userService`

| action | payload | 成功 `data` |
| --- | --- | --- |
| `bootstrap` | `{}` | `{user_id, role, status, onboarding_completed, preferred_library_view}` |
| `getProfile` | `{}` | `{user:{nickname,avatar_file_id,library_name,mother_age_range,city,preferred_library_view}, child:{child_id,nickname,birth_year_month,gender}, stats:{book_count,shelf_count,pending_review_count}}` |
| `completeOnboarding` | `{library_name, child:{nickname,birth_year_month,gender}, nickname?, avatar_file_id?, mother_age_range?, city?}` | `{user_id, child_id, onboarding_completed:true}` |
| `updateProfile` | `{user?:{nickname?,avatar_file_id?,library_name?,mother_age_range?,city?,preferred_library_view?}, child?:{nickname?,birth_year_month?,gender?}}` | 与 `getProfile` 相同 |
| `cancelAccount` | `{confirm_text:"注销账号"}` | `{status:"deleting", deletion_job_id, expected_completed_at}` |
| `restartDeletedAccount` | `{confirm:true, library_name, child:{nickname,birth_year_month,gender}}` | `{user_id, child_id, onboarding_completed:true}` |

校验：`library_name` 1—30 字；孩子昵称 1—20 字；出生年月格式 `YYYY-MM` 且不得晚于当前月；性别只允许 `female`、`male`、`unspecified`。

### 15.4 `bookService`

| action | payload | 成功 `data` |
| --- | --- | --- |
| `lookupByIsbn` | `{isbn, scan_session_id?}` | `{lookup_status:"found", edition:EditionSummary, provider_called, cache_hit}` |
| `getLookupStatus` | `{isbn}` | `{lookup_status:"querying"|"found"|"not_found"|"provider_error", edition?:EditionSummary, retry_after_ms?:integer}` |
| `searchCachedBooks` | `{query, cursor?, limit?}` | `PageResult<EditionSummary>`；空结果返回 `items:[]`，不得回源 |
| `getEditionDetail` | `{edition_id}` | `{edition:白名单完整标准字段, manual_submission?:ManualSubmissionSummary}` |
| `createManualBook` | `{title,contributors_text?,publisher?,isbn?,binding_type?,cover_file_id?}` | `{edition:EditionSummary, submission:ManualSubmissionSummary, review_eta_text:"预计 1—3 个自然日"}` |
| `updateManualSubmission` | `{submission_id,patch:{title?,contributors_text?,publisher?,isbn?,binding_type?,cover_file_id?}}` | `{edition:EditionSummary, submission:ManualSubmissionSummary}` |
| `resubmitManualBook` | `{submission_id}` | `{submission:ManualSubmissionSummary, review_eta_text:"预计 1—3 个自然日"}` |

`createManualBook`、`updateManualSubmission`、`resubmitManualBook` 是幂等写请求。仅创建者可以修改 `pending` 或 `rejected` 提交；`approved`、`merged` 不可修改。

### 15.5 `libraryService`

| action | payload | 成功 `data` |
| --- | --- | --- |
| `listBooks` | `{query?,preference?:"unmarked"|"recommended"|"neutral"|"not_recommended",bookshelf_id?,cover?:"with"|"without",sort?:"newest"|"oldest"|"title"|"custom",cursor?,limit?}` | `PageResult<UserBookSummary>` |
| `addBook` | `{edition_id,quantity_delta?:1,scan_session_id?}` | `{user_book:UserBookSummary,created:boolean,quantity_changed:boolean}` |
| `getUserBook` | `{user_book_id}` | `{user_book:UserBookSummary,private_note,bookshelves:ShelfSummary[],manual_submission?:ManualSubmissionSummary}` |
| `updateBook` | `{user_book_id,patch:{quantity?,preference?,private_note?,custom_sort?}}` | `{user_book:UserBookSummary,private_note}` |
| `removeBook` | `{user_book_id,confirm:true}` | `{removed:true,user_book_id}` |
| `batchUpdate` | `{user_book_ids:string[1..50],operation:"set_preference"|"add_to_shelf"|"remove_from_shelf"|"remove",value?:string,confirm?:boolean}` | `{processed_count,failed:[{user_book_id,code}]}` |

`quantity` 为 1—99；`private_note` 最多 500 字；删除类批量操作必须 `confirm:true`。

### 15.6 `bookshelfService`

| action | payload | 成功 `data` |
| --- | --- | --- |
| `listShelves` | `{}` | `{items:ShelfSummary[]}` |
| `createShelf` | `{name,description?}` | `{bookshelf:ShelfSummary}` |
| `updateShelf` | `{bookshelf_id,patch:{name?,description?}}` | `{bookshelf:ShelfSummary}` |
| `deleteShelf` | `{bookshelf_id,confirm:true}` | `{deleted:true,bookshelf_id}` |
| `reorderShelves` | `{items:[{bookshelf_id,sort_order}][1..50]}` | `{updated_count}` |
| `listShelfBooks` | `{bookshelf_id,cursor?,limit?}` | `PageResult<UserBookSummary>` |
| `addBooks` | `{bookshelf_id,user_book_ids:string[1..50]}` | `{added_count,already_present_count}` |
| `removeBooks` | `{bookshelf_id,user_book_ids:string[1..50]}` | `{removed_count}` |
| `reorderBooks` | `{bookshelf_id,items:[{user_book_id,sort_order}][1..50]}` | `{updated_count}` |

书架名 1—20 字、简介最多 100 字。全部 action 验证书架和藏书属于当前用户。

### 15.7 `eventService`

| action | payload | 成功 `data` |
| --- | --- | --- |
| `trackBatch` | `{events:[{event_name,event_id,occurred_at,properties}][1..20]}` | `{accepted_count,dropped_count}` |

未知事件名或属性直接丢弃；同一 `event_id` 只写一次。事件属性必须经过第 18 节白名单。

### 15.8 `adminService`

所有 action 同时验证登录身份、`status === "active"` 和 `role === "admin"`。

| action | payload | 成功 `data` |
| --- | --- | --- |
| `dashboard` | `{date_from?,date_to?}` | `{users,activation,library,isbn,manual_review}` 指标对象 |
| `listUsers` | `{status?,query?,cursor?,limit?}` | `PageResult<{user_id,status,role,library_name,book_count,created_at}>` |
| `setUserStatus` | `{user_id,status:"active"|"disabled",reason}` | `{user_id,status}` |
| `listPendingBooks` | `{status?:"pending"|"rejected",conflict_only?:boolean,cursor?,limit?}` | `PageResult<{submission:ManualSubmissionSummary,submitted_fields,existing_edition?:EditionSummary}>` |
| `reviewManualBook` | `{submission_id,decision:"approve"|"reject"|"keep_existing"|"replace_allowed_fields",rejection_reason?,approved_fields?}` | `{submission:ManualSubmissionSummary,canonical_edition_id?,migrated_user_book_count}` |
| `updateEdition` | `{edition_id,patch:{title?,contributors_text?,publisher?,publish_date_text?,binding_type?,cover_file_id?},reason}` | `{edition:EditionSummary}` |
| `retryCoverTransfer` | `{edition_id}` | `{edition_id,cover_status,cover_file_id?}` |

驳回必须提供 1—200 字 `rejection_reason`；修改标准版本必须提供 1—200 字 `reason`。`approved_fields` 只能包含 action schema 声明的标准字段。

`dashboard.data` 固定结构：

```text
users = { total, new_count, onboarding_completed_count, first_book_user_count }
activation = { book_1_ratio, book_5_ratio, book_10_ratio, shelf_creation_ratio, preference_ratio }
library = { total_user_books, average_books_per_user, total_shelves }
isbn = { cache_hit_count, provider_call_count, success_count, failure_count, quota_daily_used, quota_monthly_used }
manual_review = { pending_count, overdue_3d_count, approved_count, rejected_count, conflict_count, median_review_hours }
continuous_scan = { session_count, books_added_count, duplicate_count, skipped_count, failure_count }
```

所有 ratio 是 `0..1` 数字；分母为 0 时返回 `null`，禁止返回字符串百分比。

### 15.9 `maintenanceService`

该函数没有客户端 action，不在 `services/api.js` 暴露。定时触发任务：

- `cleanupLookupCache`：清理过期查询锁、负缓存和供应商错误缓存。
- `cleanupEphemeralData`：清理过期调用限制、幂等记录和失败临时文件。
- `processDeletionJobs`：分批执行账号数据删除。
- `reconcileCounts`：校正书架藏书数和核心指标。
- `checkProviderQuota`：检查日/月全局额度并写告警或熔断状态。

---

## 16. 分页、事务和幂等

### 16.1 分页

- 默认 `limit = 24`。
- 最大 `limit = 50`。
- 使用游标，不使用无限增长的页码偏移。
- 响应包含 `items`、`next_cursor` 和 `has_more`。
- 排序必须包含稳定的 `_id` 次级排序；游标编码排序字段、最后值、最后 `_id` 和版本。
- 游标与当前 action、用户、筛选和排序不匹配时返回 `INVALID_CURSOR`。

### 16.2 必须使用事务的操作

- 确认重复藏书数量加一。
- 删除藏书并清理书架计数。
- ISBN 查询锁获取和状态切换。
- 管理员解决 ISBN 冲突并迁移用户藏书关联。
- 原子认领幂等键。

事务只包含数据库读写，必须保持短小。阿里云请求、封面下载、内容安全检测和云存储上传全部在事务外执行，并通过状态字段衔接。

### 16.3 幂等

- 客户端为每次独立写操作生成 UUID v4 `requestId`，连续扫码的每本书使用不同 ID。
- 云函数先在事务中创建 `processing` 幂等键；键已为 `completed` 时返回保存的精简结果。
- 相同键仍为 `processing` 时返回 `REQUEST_IN_PROGRESS`；失败且可重试时写 `failed_retryable`。
- 用户藏书和书架关系使用第 4.4 节确定性文档 ID。
- 幂等键不能代替权限、字段或业务状态校验。

---

## 17. 错误码与前端处理

| 错误码 | 用户提示 | 前端动作 |
| --- | --- | --- |
| `UNAUTHENTICATED` | 登录状态异常，请重试 | 重新执行 bootstrap |
| `USER_DISABLED` | 当前账号暂时无法使用 | 进入禁用说明页 |
| `FORBIDDEN` | 你没有权限执行此操作 | 停留当前页 |
| `ACTION_NOT_FOUND` | 当前功能不可用 | 记录客户端版本并停止调用 |
| `INVALID_ARGUMENT` | 请检查填写内容 | 标记具体字段 |
| `INVALID_CURSOR` | 列表状态已更新 | 清空游标并重新加载 |
| `INVALID_ISBN` | 请输入正确的 ISBN | 保留输入焦点 |
| `BOOK_NOT_FOUND` | 暂未查到这本书 | 提供手动录入 |
| `BOOK_LOOKUP_IN_PROGRESS` | 正在获取图书信息 | 按 800/1600/2400ms 查询状态 |
| `ISBN_PROVIDER_UNAVAILABLE` | 图书服务暂时繁忙 | 提供重试和手动录入 |
| `ISBN_PROVIDER_AUTH_ERROR` | 图书服务配置异常 | 记录日志，提示稍后再试 |
| `ISBN_GLOBAL_QUOTA_EXCEEDED` | 今日图书查询额度已用完 | 只保留缓存搜索和手动录入 |
| `BOOK_ALREADY_EXISTS` | 这本书已在绘本馆中 | 显示查看或数量加一 |
| `MANUAL_REVIEW_PENDING` | 这本书正在审核中 | 显示预计 1—3 天 |
| `MANUAL_REVIEW_REJECTED` | 这本书需要修改 | 展示驳回原因和编辑入口 |
| `ISBN_CONFLICT` | 发现相同 ISBN 的图书 | 管理员进入冲突对比 |
| `CONTENT_SECURITY_REJECTED` | 这张图片无法使用 | 允许重新选择封面 |
| `REQUEST_IN_PROGRESS` | 操作正在处理中 | 禁止重复提交并轮询结果 |
| `RATE_LIMITED` | 操作太频繁，请稍后再试 | 禁用按钮短时间 |
| `NETWORK_ERROR` | 网络开小差了 | 显示重试 |
| `INTERNAL_ERROR` | 操作未完成，请重试 | 不展示技术细节 |

所有错误必须映射为友好文案。禁止直接显示 `errMsg`、堆栈或 JSON。

---

## 18. 埋点与基础指标

### 18.1 事件

- `user_bootstrap_completed`
- `onboarding_completed`
- `add_book_clicked`
- `continuous_scan_started`
- `continuous_scan_book_added`
- `continuous_scan_finished`
- `isbn_scan_succeeded`
- `isbn_cache_hit`
- `isbn_provider_called`
- `isbn_lookup_not_found`
- `manual_book_created`
- `manual_book_resubmitted`
- `manual_book_reviewed`
- `first_book_added`
- `book_count_reached_5`
- `book_count_reached_10`
- `bookshelf_created`
- `books_added_to_shelf`
- `preference_updated`
- `library_filter_used`

### 18.2 允许的事件属性

- `source`
- `scan_mode`
- `scan_session_id`
- `provider_called`
- `cache_hit`
- `result_code`
- `book_count_bucket`
- `duration_bucket`

不得把书籍私人备注、孩子出生年月、城市明文、OpenID 或 AppCode写入事件。

### 18.3 管理员基础指标

- 总用户数和新增用户数。
- 完成建馆用户数。
- 完成首本录入用户数。
- 人均藏书数。
- 录入 1、5、10 本用户比例。
- 书架创建率。
- 偏好标记率。
- ISBN 缓存命中率。
- ISBN 外部调用量、成功率和失败率。
- 连续扫码会话数、平均成功加入数量和失败率。
- 手工录入量、待审核量、审核耗时分布、通过率和驳回率。

`V1-Core` 不制作复杂图表库；管理员页使用清晰的指标卡和简单列表。当前只展示指标，不设置成功阈值、不自动输出“验证成功/失败”结论，也不因指标触发产品行为。

---

## 19. 非功能要求

### 19.1 性能

- 在近两年主流中端手机、微信正式版、4G/Wi-Fi、已有登录态条件下，绘本馆首屏 P75 在 2 秒内可交互、P95 在 3.5 秒内可交互。
- 首次只加载 24 本。
- 图片启用懒加载。
- 列表请求和搜索请求可取消或忽略过期响应。
- 支持至少 500 本个人藏书稳定浏览。
- ISBN 缓存命中云函数响应 P75 小于 1 秒、P95 小于 1.8 秒，不含用户扫码界面时间。
- 外部 ISBN 查询显示明确进度，不阻塞其他页面。

### 19.2 可靠性

- 外部 ISBN 服务失败不影响用户查看已有藏书。
- 封面转存失败不影响图书信息落库。
- 书架计数和指标聚合更新失败必须记录日志并可由维护任务校正。
- 所有云函数日志包含 `requestId`，不包含密钥和完整个人信息。

### 19.3 数据安全

- 阿里云 AppCode 只在云函数环境变量中。
- `USER_ID_SECRET` 与其他 HMAC 密钥只在云函数环境变量中，开发和生产使用不同值。
- 管理员操作写入审计日志。
- 账号注销后立即禁止登录使用，个人数据由定时云函数在 24 小时内清理。
- 可保留无法关联个人身份的汇总指标。
- 上传文件限制为 JPEG、PNG 或 WebP，最大 5 MB。
- 数据库和云函数调用安全规则必须作为仓库文件保存，并在发布清单中逐项核对。

### 19.4 运维与恢复

- 云函数配置必须在 README 中记录运行时、超时、内存、环境变量名和触发器；不记录真实密钥值。
- 生产发布前导出数据库 schema、索引和安全规则；每月至少验证一次数据库回档或导出恢复流程。
- 所有 schema 变更通过 `schema_migrations` 和脚本执行，禁止只在控制台手工修改后不留记录。
- 监控至少覆盖云函数错误率、P95 耗时、ISBN 供应商错误率、日/月额度、封面失败量、待审核积压和删除任务失败。
- 日志中敏感字段统一脱敏；普通业务日志保留期不超过 30 天，供应商排障日志不超过 14 天，审计日志按产品合规要求保留。
- 发布失败时回滚到上一个小程序版本和上一组云函数构建产物；不可逆数据迁移必须在发布前备份并提供补偿脚本。

### 19.5 合规上线前置条件

产品方在提交微信审核前必须提供并确认：

- 小程序名称、头像和简介。
- 用户协议。
- 隐私保护指引。
- 阿里云 ISBN 接口购买凭证和有效 AppCode。
- 图书封面及元数据的商业展示与缓存授权范围。
- 微信小程序类目、主体和必要备案信息。

以上属于上线材料，不阻塞本地编码，但缺失时不得声明可以正式上线。

---

## 20. GPT 实施顺序

### 阶段 1：项目骨架和设计系统

交付：

- 原生小程序目录。
- 云开发初始化。
- `tokens.wxss`、公共布局和基础组件。
- 按 `家庭数字绘本馆 UI与Figma设计规范.md` 建立变量、组件和页面骨架。
- 三个底部导航页面。
- 骨架屏、空状态、错误状态和 Toast。

验证：

- 微信开发者工具可编译。
- 三个 Tab 可正常切换。
- 组件使用统一颜色、间距和圆角。
- 不存在横向溢出、破图和过小点击区域。

### 阶段 2：身份和首次建馆

交付：

- `userService`。
- 启动页、引导页、资料页。
- `users`、`children` 集合和索引说明。

验证：

- 新用户只创建一次。
- 已建馆用户直接进入绘本馆。
- 禁用用户无法进入业务页面。
- 出生年月和字段长度校验有效。

### 阶段 3：ISBN 和图书目录

交付：

- ISBN-10/13 校验与转换。
- `bookService.lookupByIsbn`。
- 阿里云接口适配器。
- 缓存锁、负缓存和调用限制。
- 封面转存。
- 单次扫码、连续扫码和会话汇总。
- 缓存书库搜索空结果。
- 手动录入、审核状态、驳回修改和重新提交。

验证：

- 非法 ISBN 不产生付费调用。
- 首次查询回源并落库。
- 第二次查询直接命中云数据库。
- 并发相同 ISBN 不重复付费调用。
- 24 小时临时封面已转为云存储 `fileID`。
- 供应商失败时可以手动录入。
- 连续扫描新增、重复、跳过、失败和取消流程均可结束并查看汇总。
- 缓存搜索无结果时不调用付费接口。

### 阶段 4：个人绘本馆

交付：

- `libraryService`。
- 网格、列表、详情。
- 搜索、筛选、排序、偏好、备注和数量。
- 重复录入确认。

验证：

- 500 本场景可以分页。
- 视图选择被记住。
- 重复录入只增加数量。
- 私人备注只在本人 `getUserBook` 详情响应中返回，不出现在列表、标准书目或管理员指标响应。

### 阶段 5：自定义书架

交付：

- `bookshelfService`。
- 书架列表、详情、编辑和批量选书。
- 书架与藏书多对多关系。

验证：

- 一本书可加入多个书架。
- 删除书架不删除藏书。
- 删除藏书会清理相关关系。
- 排序刷新后保持。

### 阶段 6：管理员和基础指标

交付：

- `adminService`。
- 隐藏管理员入口。
- 用户状态、图书审核、驳回原因、ISBN 冲突处理、封面重试和基础指标。
- 审计日志。

验证：

- 普通用户无法调用任何管理员 action。
- 普通用户直接打开管理员路由会被重定向。
- 管理操作有审计记录。
- 冲突对比可以选择保留、替换允许字段或驳回，且不会产生两个已审核 ISBN 版本。
- 指标仅展示，不出现成功阈值或自动结论。

### 阶段 7：维护、测试和发布准备

交付：

- `maintenanceService` 定时触发。
- 单元测试、契约测试和人工验收清单。
- README、数据库索引和环境配置文档。

验证：

- 测试全部通过。
- AppCode 未出现在前端和仓库。
- 不存在未处理的 Promise 拒绝。
- 体验版完成第 21 节全部关键验收。

---

## 21. 验收标准

### 21.1 核心业务

1. 新用户可以完成建馆。
2. 用户可以扫码或输入合法 ISBN。
3. 云数据库无记录时才调用阿里云接口。
4. 查询成功后书籍信息落库。
5. 同一 ISBN 再次查询不调用付费接口。
6. 供应商临时封面已转存至微信云存储。
7. 无结果或接口失败时可以手动录入。
8. 用户可以以网格和列表查看绘本。
9. 用户可以搜索、筛选和排序。
10. 用户可以标记推荐、一般、不推荐或取消标记。
11. 用户可以创建、编辑、删除和排序书架。
12. 一本绘本可以属于多个书架。
13. 用户可以连续扫码，并在结束时查看新增、数量增加、跳过和失败汇总。
14. 缓存书库只搜索已收录图书，搜不到显示空结果且不调用供应商。
15. 手工录入显示 1—3 天审核预期；驳回后显示原因并允许修改重提。
16. 相同 ISBN 的手工提交在管理员端显示冲突，并由管理员决定保留版本。

### 21.2 隐私与权限

1. 前端代码中不存在阿里云 AppCode。
2. 普通用户不能读取或修改其他用户的私人数据。
3. 普通用户不能访问管理员接口。
4. 被禁用用户不能继续写数据。
5. 手工图书在审核前不能进入其他用户缓存搜索结果。
6. 注销后账号立即不可用并创建数据清理任务。
7. 云函数安全规则、数据库安全规则和函数内角色校验同时生效。

### 21.3 UI

1. 页面符合 `家庭数字绘本馆 UI与Figma设计规范.md` 的变量、组件、状态和原型。
2. 绘本封面为主要视觉元素。
3. 网格、列表、空状态、错误状态和骨架屏完整。
4. 页面在常见微信手机尺寸下无横向滚动和遮挡。
5. 长书名、无封面和缺少作者时布局稳定。
6. 所有主要点击区域不小于 `88rpx × 88rpx`。
7. 删除和注销有二次确认。
8. 页面不能呈现为默认表单堆叠或通用后台风格。

### 21.4 ISBN 专项

1. ISBN 校验位算法有单元测试。
2. ISBN-10 可转换为 ISBN-13。
3. ISBN-10 末位 `X` 不直接传给只接受纯数字的供应商接口。
4. 业务成功以 JSON `code === 200` 判断。
5. `details` 为空时按未找到处理。
6. 业务码 `400`、`500`、`999` 有明确映射。
7. 并发查询锁有测试。
8. 负缓存 24 小时有效。
9. 封面类型、大小和下载超时有校验。
10. 封面域名、重定向、私网 IP、魔数、解码和像素上限有测试。
11. 60 秒锁超过一次重试和封面转存的最长正常流程，且锁 token 不能被其他请求释放。

---

## 22. 测试要求

### 22.1 单元测试

至少覆盖：

- ISBN-10 校验。
- ISBN-13 校验。
- ISBN-10 转 ISBN-13。
- 供应商响应字段映射。
- 供应商错误码映射。
- 用户输入清洗。
- 权限判断。
- 确定性关系 ID。
- HMAC 用户 ID。
- 年龄段动态计算。
- 搜索前缀和分词生成。
- 游标编码、解码与筛选绑定。
- 封面 URL 和图片安全校验。
- 手工审核状态机与 ISBN 冲突决策。

### 22.2 云函数契约测试

每个 action 至少包含：

- 正常请求。
- 缺少字段。
- 字段类型错误。
- 无权限。
- 资源不存在。
- 重复请求。
- 数据库失败。

ISBN 适配器必须使用固定 Fixture，不在自动测试中真实消耗付费调用次数。

### 22.3 人工端到端测试

在微信开发者工具和至少一台真实手机完成：

- 新用户首次建馆。
- 首次 ISBN 回源。
- 相同 ISBN 缓存命中。
- 连续扫描 3 本、重复扫描、跳过失败项并正常结束。
- 缓存搜索无结果页面。
- 手动录入。
- 手工录入被驳回、查看原因、修改重提。
- 重复藏书数量加一。
- 创建书架并批量加书。
- 管理员审核普通手工图书和处理 ISBN 冲突。
- 弱网、断网和接口超时。
- 小屏 iPhone、标准 iPhone 和至少一台 Android 真机的布局与点击区域。

### 22.4 安全与并发测试

- 普通用户调用每个管理员 action 均被拒绝。
- 客户端伪造 `owner_id`、`role`、`user_id` 或未知字段均被拒绝。
- 相同 `requestId` 并发提交只产生一次写入。
- 20 个并发相同 ISBN 请求最多产生一次供应商调用。
- 恶意封面 URL、重定向到私网、伪造 MIME 和超大图片均被拒绝。
- 注销任务可中断续跑，重复执行不会删除其他用户数据。

---

## 23. 完成定义

只有同时满足以下条件，才能声明 `V1-Core` 编码完成：

- 第 3.1 节功能全部实现。
- 第 21 节验收全部通过。
- 自动测试无失败。
- 微信开发者工具编译无错误。
- 真实手机核心流程通过。
- 数据库索引和安全规则已配置。
- 云函数构建脚本生成的每个部署包都包含 `_shared`，且无部署目录外依赖。
- 阿里云 AppCode 只存在于云函数环境变量。
- README 包含本地启动、云环境初始化、云函数部署、索引创建和发布步骤。
- README 包含运行时、超时、内存、全部环境变量名、定时触发器、备份恢复和回滚步骤。
- 不存在未完成标记、模拟数据入口、硬编码用户 ID 或硬编码管理员身份。
- 不存在 `shareService`、分享页面、分享集合，以及社区、支付、推荐和交易功能。

---

## 24. 产品闭环与后续衔接

```text
V1-Core 当前闭环：
家长逐步录入已有绘本
→ 云数据库复用已查询的标准图书信息
→ 形成美观、统一的数字绘本馆
→ 按家庭习惯建立书架和偏好
→ 继续回来补充和整理绘本

V1-Share 下一阶段增加：
从绘本馆或书架创建主动分享
→ 获得浏览与点赞反馈
→ 分享访客创建自己的绘本馆
→ 用户继续回来补充和整理绘本
```

编码助手到达 `V1-Core` 完成定义后必须停止，不得自动开始 `V1-Share`。
